# Зависимости добавляем по фазам, а не все сразу —
# так видно, какая библиотека для чего появилась.

# Фаза 0: типизированная конфигурация из .env
pydantic-settings>=2.6,<3

# Фаза 1: приём заявок по HTTP
fastapi>=0.115,<1          # веб-фреймворк: роутинг + валидация через pydantic + авто-документация
uvicorn[standard]>=0.32,<1 # ASGI-сервер, который слушает порт и запускает приложение
python-multipart>=0.0.12   # разбор данных из HTML-форм (их шлют Tilda, WordPress и прочие)
