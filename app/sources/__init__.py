"""Адаптеры источников.

Каждый модуль здесь — переводчик с языка конкретного канала на язык системы.
Он знает всё про формат своего источника и ничего про то, что происходит с
лидом дальше.

Контракт для любого нового источника ровно один:

    вернуть объект `Lead` (см. app/domain/lead.py)

Пока адаптер его соблюдает, остальная система его не заметит. Это и есть
"единая точка входа": добавление канала = добавление файла в эту папку.
"""
